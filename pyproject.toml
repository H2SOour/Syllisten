[project]
name = "syllisten"
version = "4.3.3"
description = "Multilingual focused-listening and dictation web app"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = [
  "streamlit>=1.45,<2",
  "openai>=2.0,<3",
  "edge-tts>=7.0,<8",
]

[tool.ruff]
line-length = 120
